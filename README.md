# projeto-docker-vagrant-ansible
Projeto prático utilizando Vagrant, Ansible e Docker para provisionamento automatizado de uma VM Debian com ambiente WordPress completo (Nginx + WordPress + MySQL), executado sobre VirtualBox.
